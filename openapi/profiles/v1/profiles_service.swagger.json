{
  "swagger": "2.0",
  "info": {
    "title": "Videocoin api",
    "version": "1.0"
  },
  "schemes": [
    "https"
  ],
  "consumes": [
    "application/json"
  ],
  "produces": [
    "application/json"
  ],
  "paths": {
    "/api/v1/profiles": {
      "get": {
        "summary": "This API call returns a list of supported output transcoding profiles. Most notably, the items in the response list the ID of a profile which is required when creating a new stream object.",
        "operationId": "List",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1ProfileListItems"
            }
          },
          "401": {
            "description": "Returned when the user is not logged in.",
            "schema": {}
          },
          "403": {
            "description": "Returned when the user does not have permission to access the resource.",
            "schema": {}
          },
          "404": {
            "description": "Returned when the resource does not exist.",
            "schema": {
              "example": {
                "message": "Not found",
                "fields": null
              }
            }
          }
        },
        "tags": [
          "ProfilesService"
        ]
      }
    }
  },
  "definitions": {
    "v1ProfileListItem": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "description": {
          "type": "string"
        },
        "is_enabled": {
          "type": "boolean",
          "format": "boolean"
        }
      }
    },
    "v1ProfileListItems": {
      "type": "object",
      "properties": {
        "items": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/v1ProfileListItem"
          }
        }
      }
    },
    "v1RenderResponse": {
      "type": "object",
      "properties": {
        "render": {
          "type": "string"
        }
      }
    }
  },
  "securityDefinitions": {
    "ApiKeyAuth": {
      "type": "apiKey",
      "name": "Authorization",
      "in": "header"
    }
  },
  "security": [
    {
      "ApiKeyAuth": []
    }
  ]
}
